Phase~A2 showed that the raw angle field is rotational. Here we use the more relevant scalar: the Meta-MAPG-minus-PG update projected onto the direction from the current policy to $(C,C)$. Positive values mean the peer correction directly helps basin entry. In the central mixed region, the mean projection is 0.0213 and is positive in 100\% of cells.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{phase_p_projection}
  \caption{Peer-correction projection toward $(C,C)$. The left panel is the scalar projection; the right panel colours the difference quiver by the same projection. This is the mechanism figure used for the basin-entry claim.}
  \label{fig:phase-p-projection}
\end{figure}
